document.addEventListener('DOMContentLoaded',()=>{const t=document.querySelector('[data-total]');if(t){document.querySelectorAll('[data-cost]').forEach(i=>i.addEventListener('input',()=>{let n=[...document.querySelectorAll('[data-cost]')].reduce((s,x)=>s+(+x.value||0),0);t.textContent='₹'+n.toLocaleString('en-IN');document.querySelector('[data-remaining]').textContent='₹'+Math.max(0,(+document.querySelector('[data-budget]').value||0)-n).toLocaleString('en-IN')}))}});
